let username: string = "Sesac";
console.log(`Username: ${username}`);

let age: number = 30;
console.log(`Age: ${age}`);

let isAdmin: boolean = true;
console.log(`IS Admin? ${isAdmin}`);

let unknownType: any = "아무거나 가능";
console.log(`UnknownType (any): ${unknownType}`);

let undefinedType: undefined =  undefined;
console.log(`Undefined Type: ${undefinedType}`);

let nullValue: null = null;
console.log(`Null Type: ${nullValue}`);

let notype;
notype = 1;
notype = 'a';
